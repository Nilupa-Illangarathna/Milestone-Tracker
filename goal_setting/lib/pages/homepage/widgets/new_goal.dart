import 'package:flutter/material.dart';

class NewGoalPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('New Goal'),
      ),
      body: Center(
        child: Text('New Goal Page'),
      ),
    );
  }
}